spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///D:/Programming/IdeaProjects/hero-cloud/repo-config
server:
  port: 8888